:root{
  --bg:#0b0c10; --card:#15171c; --text:#f2f5f7; --muted:#98a2b3; --accent:#70e1b8; --pill:#23262f;
}
*{box-sizing:border-box}
html,body{margin:0;padding:0;font-family:-apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Apple SD Gothic Neo,'Noto Sans KR','Malgun Gothic',sans-serif;color:var(--text);background:var(--bg);line-height:1.5}
a{color:var(--accent);text-decoration:none}
.section{padding:64px 20px;max-width:1100px;margin:0 auto}
.hero{padding:96px 20px;text-align:center}
.hero h1{font-size:2.2rem;margin:0 0 12px}
.btn{display:inline-block;margin-top:12px;padding:10px 16px;background:var(--accent);color:#051215;border-radius:8px;font-weight:600}
.site-header{position:sticky;top:0;display:flex;align-items:center;gap:16px;justify-content:space-between;padding:12px 16px;background:rgba(11,12,16,.8);backdrop-filter:saturate(140%) blur(8px);border-bottom:1px solid #1d2129;z-index:10}
.brand .logo{font-weight:800;letter-spacing:.5px;margin-right:8px}
.brand .tagline{font-size:.9rem;color:var(--muted)}
.nav-toggle{display:none}
.nav-list{display:flex;gap:14px;list-style:none;margin:0;padding:0}
.nav-list a{padding:6px 8px;border-radius:6px}
.nav-list a:hover{background:#1a1d24}
.theme-toggle{background:transparent;border:none;font-size:18px;color:var(--text);cursor:pointer}
.grid{display:grid;gap:20px}
.grid.two{grid-template-columns:1fr; }
@media(min-width:900px){ .grid.two{grid-template-columns:1fr 1fr} }
.table{width:100%;border-collapse:collapse;background:var(--card);border-radius:12px;overflow:hidden}
.table th,.table td{padding:10px 12px;border-bottom:1px solid #23262f;text-align:left}
.table th{background:#1a1d24}
.pill{display:flex;flex-wrap:wrap;gap:8px;padding-left:0;list-style:none}
.pill li{background:var(--pill);padding:6px 10px;border-radius:99px}
.check{padding-left:0;list-style:none}
.check li::before{content:"✓ ";color:var(--accent)}
.card{background:var(--card);padding:12px 14px;border-radius:12px;overflow:auto}
.small{font-size:.9rem}
.muted{color:var(--muted)}
.site-footer{padding:28px 16px;text-align:center;color:var(--muted);border-top:1px solid #1d2129}
.accordion details{background:var(--card);border-radius:10px;margin:8px 0;padding:8px 12px}
.accordion summary{cursor:pointer;font-weight:600}
@media(max-width:760px){ .nav-toggle{display:block;background:transparent;border:1px solid #2a2f3a;color:var(--text);padding:6px 10px;border-radius:8px}
  .nav-list{display:none;flex-direction:column;background:#0f1218;position:absolute;right:10px;top:56px;padding:12px;border:1px solid #23262f;border-radius:12px}
  .nav-list.open{display:flex}
}
